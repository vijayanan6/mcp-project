anthropic[mcp]>=0.30.0
mcp>=1.0.0
pypdf>=4.0.0
pymupdf>=1.24.0
pytesseract>=0.3.10
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
chromadb>=0.4.0
sentence-transformers>=2.7.0
python-dotenv>=1.0.0
httpx>=0.27.0
langfuse>=4.14.0

# Optional, not installed by default — only needed if SPENDGAUGEAI_URL/SPENDGAUGEAI_API_KEY
# are set in .env (see README.md "Optional: SpendGaugeAI reporting"). The app runs fine
# without it; api.py handles ImportError and disables the feature cleanly.
# spendgaugeai
