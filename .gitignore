# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Environment & secrets
.env
.env.*
*.key

# Sensitive documents — never commit personal files
docs/*.pdf
docs/*.txt
docs/*.docx

# Virtual environments
venv/
.venv/
env/

# IDE
.vscode/
.idea/
*.suo

# Database (local only — not committed)
data.db

# ChromaDB vector database (local only)
chroma_db/

# OS
.DS_Store
Thumbs.db

# LinkedIn drafts — personal, not project code
LINKEDIN_POSTS.md

# Logs
*.log
logs/

# Temp files
*.tmp
*.temp

# Claude Code internal files
.claude/

# Testing (pytest — Phase pre-Docker)
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# Type checking
.mypy_cache/

# GCP — service account keys (NEVER commit these)
*service-account*.json
*-key.json
service_account.json
gcp-key.json

# Certificates & private keys
*.pem
*.p12
*.pfx

# Docker local overrides (Phase 1)
docker-compose.override.yml
.docker/

# React / Node (Phase 2)
node_modules/
.next/
.env.local
.env.development.local
.env.production.local
frontend/dist/
frontend/build/
